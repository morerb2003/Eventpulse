package com.event.util;

import org.springframework.stereotype.Component;

@Component
public class JwtUtils extends com.event.security.JwtUtil {
    // This is an alias class to satisfy the requested directory structure
}
